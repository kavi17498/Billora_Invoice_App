import 'package:flutter/material.dart';

class EstimationsPage extends StatelessWidget {
  const EstimationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Estimations Page",
        style: TextStyle(fontSize: 20, color: Colors.grey),
      ),
    );
  }
}
